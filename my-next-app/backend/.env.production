# Production — Set these as real environment variables on your server/container
# NEVER commit actual values to git
MONGO_URI=mongodb+srv://<db_user>:<db_password>@<cluster>.mongodb.net/?appName=<AppName>
JWT_SECRET=your_strong_production_jwt_secret
PORT=5000
